%p#notice= notice
%h1 Enrollments
%table
  %thead
    %tr
      %th Course
      %th User
      %th Rating
      %th Review
      %th Price
      %th{:colspan => "3"}
  %tbody
    - @enrollments.each do |enrollment|
      %tr
        %td= enrollment.course_id
        %td= enrollment.user_id
        %td= enrollment.rating
        %td= enrollment.review
        %td= enrollment.price
        %td= link_to 'Show', user_enrollment_path(@user, enrollment)
        %td= link_to 'Edit', edit_user_enrollment_path(@user, enrollment)
        %td= link_to 'Destroy', user_enrollment_path(@user, enrollment), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Enrollment', new_user_enrollment_path(@user)
