%p#notice= notice
%h1 Enrollments
%table.table
  %thead
    %tr
      %th Course
      %th User
      %th Rating
      %th Review
      %th Price
      %th{:colspan => "3"}
  %tbody
    - @enrollments.each do |enrollment|
      %tr
        %td= enrollment.course_id
        %td= enrollment.user_id
        %td= enrollment.rating
        %td= enrollment.review
        %td= enrollment.price
        %td= link_to 'Show', course_path(enrollment.course_id), class: 'btn btn-outline-info btn-sm'
        %td= link_to 'Edit', edit_enrollment_path(enrollment), class: 'btn btn-outline-success btn-sm'
        %td= link_to 'Destroy', enrollment_path(enrollment), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm'
